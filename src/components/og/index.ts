/**
 * @level9/brand/components/og — satori-compatible OG image primitives.
 *
 * Drop-in layout for Next.js App Router `opengraph-image.tsx` files so
 * every site produces a brand-consistent social share card from a single
 * SITE_META preset (see @level9/brand/content/siteMeta).
 */

export { OgCard } from "./OgCard";
export { OgMark } from "./OgMark";
export type { OgCardProps } from "./OgCard";
